# 맥북 작업인계

## 기준 상태

- Repository: `https://github.com/Victor20000130/CardBoardGame.git`
- 로컬 경로: `/Users/simda/Developer/CardBoardGame`
- Branch: `main`
- 기준 Commit: `9697dc7`
- Unity 프로젝트: `/Users/simda/Developer/CardBoardGame/CardBoardGame`
- Unity 버전: `6000.0.50f1 (f1ef1dca8bff)`

## 동기화 원칙

GitHub를 기준 원본으로 사용한다. Windows와 Mac에서 같은 브랜치를 장시간 동시에 수정하지 않는다.

작업 시작은 `AGENTS.md` 확인 → `git status` → Remote 확인 → `git fetch` → 안전한 Pull 순서로 진행한다. 작업 종료는 Test/실행 → Diff/Secret/Generated 검사 → Commit → Push → Remote SHA 확인 순서로 진행한다.

## macOS 호환성

- C# 소스에서 Windows 절대 경로 및 명시적인 Windows Runtime 분기는 발견되지 않았다.
- Unity Plugin DLL은 관리형 Unity 플러그인으로 포함되어 있으며 Editor Import/Compile로 최종 호환성을 검증한다.
- 런타임 스크립트의 불필요한 Editor 전용 import를 제거하거나 `UNITY_EDITOR` 조건으로 분리했다.
- 추적 중이던 `.DS_Store` 2개를 제거하고 재발 방지 규칙을 추가했다.
- Unity Test Framework 테스트 케이스: `NOT_AVAILABLE` (0개)
- Unity Package 복구: 성공
- 최초 Import: 성공
- Script Compile: 성공
- macOS Player Build: 성공
- 실제 실행: 성공 (`LobbyScene`, Apple M3 Metal)
- 런타임 치명적 오류 및 누락 자산: 발견되지 않음

## 주의사항

- Unity `6000.0.50f1`은 Unity Hub에서 알려진 보안 경고가 표시된다. Windows와 버전을 맞추기 위해 유지했으며 배포 전 업그레이드 검토가 필요하다.
- 비영어권 Editor 언어에서 Pixel Perfect Script Updater 오류가 발생하므로 이 Mac의 Unity Editor 언어는 English로 설정했다.
